# Railway → DigitalElevate (web) service → Variables → "Import from repo" or paste in RAW Editor.
# Replace "Postgres" with your PostgreSQL service name (sidebar, case-sensitive).
DATABASE_URL=${{ Postgres.DATABASE_URL }}
